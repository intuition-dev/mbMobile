html
   head
      script(src='//unpkg.com/axios@0.19.0-beta.1/dist/axios.min.js')
      script(src='//cdn.jsdelivr.net/npm/jquery@3.3.1/dist/jquery.slim.min.js')
      script(src='/spa-router.js')
   body
      p
      a(href='/page/index1/index.html#aa') 1 o
      hr
      a(href='/page/index2/index.html?q=2') 2 t
      p
      #router
         p oh hi 1
         script.
            console.info('1 sc');
      footer
         p f
      script.
         //- SPA Ex:
         SPArouter.init(onNavigate);
         function onNavigate (evt) {
            if (evt.detail.type == SPArouter.NavSTART) { //start
               //$('#router').fadeTo(100,.2);
            }
            else if (evt.detail.type == SPArouter.NavDONE) {
               $(SPArouter.zone).html(evt.detail.newContent);
               //$('#router').fadeTo(100,1);
               window.scrollTo(0, 0);
            }
         }
